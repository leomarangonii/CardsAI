{
  "name": "cardsai-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "corepack pnpm build && firebase emulators:start --only auth,firestore,functions",
    "deploy": "firebase deploy --only functions,firestore"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.106.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
